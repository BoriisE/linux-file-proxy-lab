# linux-file-proxy-lab
Linux project with file server setup (Samba, CIFS, autofs) and Nginx proxy with load balancing.

The project focuses on configuring a file server and a proxy server in a Linux environment. It provides centralized access to file resources via CIFS, automatic directory mounting using autofs, and load balancing between backend servers using Nginx.
